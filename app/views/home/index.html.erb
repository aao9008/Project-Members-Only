<h1>Welcome to My Application</h1>

<% if user_signed_in? %>
  <p>
    Welcome, <%= current_user.username %>!
    <%= link_to 'Logout', destroy_user_session_path, method: :delete, data: { turbo_confirm: "Are you sure?", turbo_method: :delete } %>
  </p>
  
<% else %>
  <p>Welcome, Guest!</p>
<% end %>

